import 'package:flutter/widgets.dart';

class P {
  static const EdgeInsets icon = EdgeInsets.symmetric(
    vertical: 9,
    horizontal: 1,
  );
  static const EdgeInsets fieldTight = EdgeInsets.all(1);
  static const EdgeInsets field = EdgeInsets.all(2);
  static const EdgeInsets fieldBig = EdgeInsets.all(4);

  static const double lineSpacing = 3;
}
